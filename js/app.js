(async function(){const cfg=await Utils.readJSON('config.json');const banner=Utils.el('#cookie-banner');const txt=Utils.el('#cookie-text');txt.textContent=cfg.site.cookie.text+' Browser: '+navigator.userAgent;Utils.el('#cookie-accept').addEventListener('click',()=>banner.classList.add('hidden'));if(cfg.ui.blockContextMenu)document.addEventListener('contextmenu',e=>e.preventDefault());})();